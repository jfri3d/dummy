kind: Module
type: container
name: a
exclude:
  - '*'
image: alpine:latest
services:
  - name: a
    command: ["/bin/sh", "-c", "sleep 600"]
---
kind: Module
type: container
name: b
exclude:
  - '*'
build:
  dependencies:
    - name: a
image: alpine:latest
tests:
  - name: int
    dependencies:
      - a
    command: ["/bin/sh","-c"]
    args: ['echo "b"']
