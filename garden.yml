kind: Project
name: dummy
environments:
  - name: dev
defaultEnvironment: dev
modules:
  include:
    - "containers/*"
providers:
  - name: kubernetes
    context: ${var.CONTEXT}
    namespace: dummy
    buildMode: cluster-docker
    environments:
      - dev
dotIgnoreFiles:
  - .gardenignore
